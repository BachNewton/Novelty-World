// TODO: Implement RTCPeerConnection wrapper
// This will wrap the browser's RTCPeerConnection API:
// - Configure ICE servers (STUN/TURN)
// - createOffer() / createAnswer(offer)
// - addIceCandidate(candidate)
// - send(data) via RTCDataChannel
// - onMessage(callback)
// - close()

export {};
