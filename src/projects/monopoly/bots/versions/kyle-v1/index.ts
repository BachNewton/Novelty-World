// kyle-v1 — the first version of the KYLE lineage, a new bot family distinct
// from claude / jane / gemini and the paradigm lines (see METHOD.md "Bot
// lineages"). Authored by Kyle. Branched from nothing — an independent,
// from-scratch policy that currently defers to the engine default everywhere
// (see ./policy.ts).
export { policy as kyleV1Bot } from "./policy";
